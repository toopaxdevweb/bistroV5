{% extends 'base.html.twig' %}

{% block body %}

    <h1 class="text-center">{{ingredientName}}</h1>
    {% for recette in targetRecette %}
            <a href="/recettes/{{recette.id}}"><p>{{ recette.nom }}</p></a>
    {% endfor %}
{% endblock %}
