{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Tarif par Budget</h1>
    {% for budgets in budget %}
            <a href="/recettes/price/{{budgets.id}}"><p>{{budgets.nom}}</p></a>
    {% endfor %}
{% endblock %}